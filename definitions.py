# Grain form dictionary
def grain_form_name(grain_form):
    grain_form_dict = {
        "0": "undefined",
        "1": "rounded",
        "2": "angular",
        "3": "faceted",
    }
    return grain_form_dict[grain_form]

# Weather conditions dictionary

# Density from grain form df


